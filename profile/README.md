# git-shelf

物語はコミットされ、分岐し、時にマージされる。

ここでは、Gitの仕組みそのものが語り手になる。
本文だけが物語ではない。
履歴も、差分も、無視されたファイルも。

`git log` は、上位存在の日記かもしれない。

---

Gitのコミット履歴、ブランチ、`.gitignore` などの仕組みを物語の構造として活用する実験的な創作プロジェクトです。

各リポジトリが一つの作品です。本文だけでなく `git log --oneline --graph --all` で履歴を眺めてみてください。
